# SaMoR — Cross-Topology Motion Representation

Project page for **SaMoR** (SABER, previously). See `index.html`.

Assets:
- `static/` — page CSS/JS + hero teaser gifs
- `supplementary_material/` — per-section gifs (composition, editing, generation, mesh_animation, transfer)
